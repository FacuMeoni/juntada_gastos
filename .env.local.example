# Supabase — copiá este archivo a `.env.local` y completá con tus claves.
NEXT_PUBLIC_SUPABASE_URL="https://TU-PROYECTO.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="tu-anon-key"

# URL pública del sitio (fallback SSR; en el browser se usa window.location.origin).
NEXT_PUBLIC_SITE_URL="http://localhost:3001"
